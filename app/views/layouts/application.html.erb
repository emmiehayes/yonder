<!DOCTYPE html>
<html>
  <head>
    <title>Yonder</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.0/css/all.css" integrity="sha384-lKuwvrZot6UHsBSfcMvOkWwlCMgc0TaWr+30HWe3a4ltaBwTZhyTEggF5tJv8tbt" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
</head>

<body>
  <!-- Nav Bar -->
  <header class="navbar is-dark"> 
    <div class="navbar-menu">
      <div class="navbar-start">
        <% if current_user %>
          <div class="navbar-item">
            Signed in as <%= current_user.first_name %>
          </div>
        <% end %>
        </div>
      <div class="navbar-end">
        <% if current_user %>
         <div class="navbar-item">
            <a class="control"><%= button_to 'Dashboard', dashboard_path, method: :get, class: "button is-outlined is-medium is-primary" %></a>
          </div>
          <div class="navbar-item">
            <a class="control"><%= button_to 'Create Activity', new_user_activity_path(current_user), method: :get, class: "button is-outlined is-medium is-danger" %></a>
          </div>
          <div class="navbar-item">
            <a class="control"><%= button_to 'Sign Out', logout_path, method: :delete, class: "button is-outlined is-medium is-warning" %></a>
          </div>
        <% else %>
          <div class="navbar-item">
            <a class="control"><%= button_to 'Sign in with Google', '/auth/google_oauth2', method: :get, class: "button is-outlined is-medium is-warning" %></a>
          </div>
        <% end %>
        </div>
      </div>
    </div>
  </header>
  
   <!--Flash -->
    <% flash.each do |key, msg| %>
    <% if key == 'alert' %>%
      <div class="notification is-danger">
        <%= content_tag :div, msg, id: key %>
    <% elsif key == 'success' %>
      <div class="notification is-success">
        <%= content_tag :div, msg, id: key %>
    <% end %>
    </div>
    <% end %>

    <%= yield %>
</body>
</html>
